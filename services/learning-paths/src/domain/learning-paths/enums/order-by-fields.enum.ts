/**
 * @description This enum contains fields that paths can be ordered by when sorting.
 */
export enum LearningPathsOrderByFields {
	CREATED_AT = 'createdAt',
	UPDATED_AT = 'updatedAt',
	NAME = 'name',
}
