export enum ActivitiesApiConstraints {
	MAX_NAME_LENGTH = 255,
	MAX_DESCRIPTION_LENGTH = 1024,
	DEFAULT_LIMIT = 100,
	MAX_LIMIT = 200,
	MIN_LIMIT = 5,
	DEFAULT_PAGE = 0,
	MIN_PAGE = 0,
}

export enum ActivityProgressApiConstraints {
	MAX_LIMIT = 100,
	DEFAULT_LIMIT = 50,
	MIN_LIMIT = 10,
	DEFAULT_PAGE = 0,
	MIN_PAGE = 0,
}
