export enum LessonsApiConstraints {
	DEFAULT_LIMIT = 100,
	MAX_LIMIT = 200,
	MIN_LIMIT = 5,
	DEFAULT_PAGE = 0,
	MIN_PAGE = 0,
	MAX_NAME_LENGTH = 255,
	MAX_DESCRIPTION_LENGTH = 1024,
}

export enum LessonProgressApiConstraints {
	MAX_LIMIT = 100,
	DEFAULT_LIMIT = 50,
	MIN_LIMIT = 10,
	DEFAULT_PAGE = 0,
	MIN_PAGE = 0,
}
