/**
 * @description This enum represents domain constraints for path entity like field sizes (or max lengths).
 */
export enum PathConstraints {
	MAX_NAME_LENGTH = 255,
	MAX_DESCRIPTION_LENGTH = 1024,
}
