syntax = "proto3";

package learning_paths.v1;

enum LearningPathsApiErrorCodes {
  LEARNING_PATH_CANNOT_BE_REMOVED = 0;
  LEARNING_PATH_NOT_FOUND = 1;

  SECTION_NOT_FOUND = 2;
  SECTION_CANNOT_BE_REMOVED = 3;

  UNIT_NOT_FOUND = 4;
  UNIT_CANNOT_BE_REMOVED = 5;

  LESSON_NOT_FOUND = 6;
  LESSON_CANNOT_BE_REMOVED = 7;

  INTERNAL_ERROR = 8;
}